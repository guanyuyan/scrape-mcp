# 安全与合规

## 严重漏洞披露

任何访问控制绕过、凭证泄露、可导致任意代码执行的缺陷，或对本项目用户的重大安全问题，
请**不要**公开在 issue 中。请提交 GitHub Security Advisory 或发送邮件到维护者。

非安全性的普通 bug 走常规 issue 即可。

## 反爬与自动化边界

本项目用于"让 AI 读取网页正文改善效率"这一正当目的。使用者应遵守：

- 目标站点的 `robots.txt`、服务条款（ToS）与法律法规
- 控制抓取频率，避免对目标站点造成可用性影响
- 仅抓取你有权访问的内容；登录态仅作用于你自有账号

本项目提供的反爬对抗能力是**自动化通用设施**，不针对任何具体网站绕过付费墙或授权控制。
使用者自担因滥用产生的合规与法律风险。

## 本地凭据安全

- 登录态（cookie/localStorage）与缓存默认存放于用户目录下的 `~/.scrape_mcp/`，**不在仓库内**。
- 请勿把该目录内容提交到任何仓库或分享给他人。